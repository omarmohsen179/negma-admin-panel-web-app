# Paste into Plesk → Git → Additional deployment actions (one line).
# Runs from the deployment directory (e.g. admin.npm.khalidelewa.com).
# Adjust httpdocs if your document root folder name differs.
#
# DISABLE_ESLINT_PLUGIN=true: avoids CRA build failure on Windows when ESLint sees the same
# path twice with different casing (e.g. C:\Inetpub\... vs C:\inetpub\...) — "Plugin react was conflicted".

npm install --force && npx cross-env DISABLE_ESLINT_PLUGIN=true REACT_APP_API_BASE_URL2=http://localhost:5000 REACT_APP_API_BASE_URL=https://clinic-api.khalidelewa.com REACT_APP_IMAGE_BASE_URL=http://localhost:5000 REACT_APP_ENV=negma REACT_APP_GOOGLE_SHEET_CORS_PROXY="https://corsproxy.io/?" npm run build && xcopy /E /Y build\* httpdocs\
